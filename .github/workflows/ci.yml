name: ci

on:
  push:
    branches:
      - main 

jobs:
  appium-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install 

      - name: Run Appium Tests
        run: |
          
          export BROWSERSTACK_USERNAME=andriistetsula_MF80wb
          export BROWSERSTACK_ACCESS_KEY=15zaPWx574J8caWwBo2R
          export BROWSERSTACK_APP_ID=bs://f604966c194d44c64d1285e7dc6b9ec2216efe7f
          
          npm run test:android:bs 