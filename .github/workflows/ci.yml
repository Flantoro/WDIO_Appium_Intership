name: BrowserStack Tests

on:
  push:
    branches:
      - main

jobs:
  test:
    name: Run tests on BrowserStack
    runs-on: ubuntu-latest

    env:
      BROWSERSTACK_USERNAME: andriistetsula_MF80wb
      BROWSERSTACK_ACCESS_KEY: 15zaPWx574J8caWwBo2R
      BROWSERSTACK_APP_ID: bs://f604966c194d44c64d1285e7dc6b9ec2216efe7f

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install

      - name: Run BrowserStack tests
        run: npm run test:android:bs